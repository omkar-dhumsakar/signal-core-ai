apiVersion: v1
kind: Service
metadata:
  name: signal-core-api-service
  labels:
    app: signal-core-api
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: signal-core-api
