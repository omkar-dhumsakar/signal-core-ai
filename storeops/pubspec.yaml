name: storeops
description: "StoreOps — Signal Core AI mobile app for Goa Bagayatdaar inventory management."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  provider: ^6.1.1
  http: ^1.2.0
  sqflite: ^2.3.0
  path: ^1.9.0
  intl: ^0.19.0
  file_picker: ^8.0.0
  shared_preferences: ^2.2.0
  google_sign_in: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
